// Treasurer reuses the admin Penalty Wallet page so they can initiate
// withdrawals (treasurer accounts also hold the admin/super_admin role).
export { default } from "@/pages/admin/PenaltyWallet";
